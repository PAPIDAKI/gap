<%= form_for([@clitem,@instruction]) do |f| %>
  <% if @instruction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@instruction.errors.count, "error") %> prohibited this instruction from being saved:</h2>

      <ul>
      <% @instruction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <strong>Compliance Criteria</strong><br>
    <%= @clitem.compliance_criteria%>
    <%= f.label :description %><br>
    <%= f.text_area :description ,:size=> "20x10" %>
  </div>
  <div class="actions">
    <%= f.submit %> | <%= link_to 'Cancel', clitem_instructions_path %>
  </div>
<% end %>
